#include<bits/stdc++.h>
#define nl "\n"
#define fori(i) for (int i = 1; i <= num; i++)
#define yes cout << "YES"; return 0;
#define no cout << "NO"; return 0;
#define forj(j) for (int j = i+1; j < n; j++)
#define fio ios_base::sync_with_stdio(0);cin.tie(0);
typedef long long int lli;
typedef long int li;
typedef long double llf;
using namespace std;
int main()
{
    fio;
    int num;
    cin >> num;
    fori(i)
    {
        if((i*i+i)/2 != num)
            continue;
        else yes
    }
    no
}


